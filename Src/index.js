import React from "react";
import ReactDOM from "react-dom/client";
import MonsterForge3D from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MonsterForge3D />
  </React.StrictMode>
);
